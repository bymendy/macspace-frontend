import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.macsecurite.macspace',
  appName: 'MacSpace',
  webDir: 'dist/macspace-frontend/browser'
};

export default config;
